name: "access_policy removed and consumer added - requires manual review"
description: "Removing access_policy while adding a consumer in the same mart should trigger manual review"

expected:
  decision: ManualReview
  reason: "One or more files require manual review"
  approved: false

  comment_contains:
    - "access_policy changes require manual review"

mr_metadata:
  title: "Remove access_policy and add reporting consumer"
  author: "testuser"
  source_branch: "feature/replace-access-policy-with-consumer"
  target_branch: "main"
