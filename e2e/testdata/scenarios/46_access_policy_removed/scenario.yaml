name: "access_policy removed from product.yaml - requires manual review"
description: "Removing access_policy from a product.yaml should trigger manual review"

expected:
  decision: ManualReview
  reason: "One or more files require manual review"
  approved: false

  comment_contains:
    - "access_policy changes require manual review"

mr_metadata:
  title: "Remove access_policy from analytics marts"
  author: "testuser"
  source_branch: "feature/remove-access-policy"
  target_branch: "main"
